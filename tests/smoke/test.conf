# ABOUTME: Minimal honeyd config for smoke testing
# ABOUTME: Creates one virtual host that responds to ICMP ping

# Create a simple template that responds to ping
create default
set default default tcp action block
set default default udp action block
set default default icmp action open

# Bind our test IP to the default template
bind 192.0.2.100 default
